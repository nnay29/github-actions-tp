name: CI - Linting & Type Checking

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
    branches: [main]
  
jobs:
  lint_and_build:
    runs-on: ubuntu-latest
    
    permissions:
      pull-requests: write
      contents: write
    
    steps:
      - name: ⬇️ Checkout code
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔍 Run ESLint (Generate Report)
        id: linter_report
        run: npm run lint:ci || true

      - name: 🔨 Run TypeScript Build Check
        run: npm run build
      
      - name: 🤖 Post Dynamic AI Feedback on Failure
        id: ai_review_step
        if: github.event_name == 'pull_request' && (success() || failure())
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          OWNER: ${{ github.repository_owner }}
          REPO: ${{ github.event.repository.name }}
        run: |
          if [ ! -s eslint_report.json ]; then
            echo "ESLint report file is vide ou n'existe pas. Aucune erreur trouvée."
            echo "ai_review<<EOF_AI_REVIEW_OUTPUT" >> $GITHUB_OUTPUT
            echo "Aucune erreur de Linting trouvée. ✅" >> $GITHUB_OUTPUT
            echo "EOF_AI_REVIEW_OUTPUT" >> $GITHUB_OUTPUT
            exit 0
          fi

          LINT_ERRORS=$(cat eslint_report.json | jq -c .)
          
          # Create JSON payload with proper escaping
          cat > request.json << EOF
          {
            "contents": [
              {
                "parts": [
                  {
                    "text": "Tu es un relecteur de code expert en React et TypeScript. Analyse ce rapport ESLint et fournis une critique concise en français. Inclue un lien vers une ressource éducative. Rapport: $LINT_ERRORS"
                  }
                ]
              }
            ]
          }
          EOF
          
          API_RESPONSE=$(curl -s -X POST "https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=$GEMINI_API_KEY" \
            -H "Content-Type: application/json" \
            -d @request.json)
            
          echo "Réponse API brute reçue (Vérifiez les erreurs 400 ou 500) :"
          echo "--------------------------------------------------------"
          echo "$API_RESPONSE"
          echo "--------------------------------------------------------"
            
          GEMINI_TEXT=$(echo "$API_RESPONSE" | jq -r '.candidates[0].content.parts[0].text' 2>/dev/null)
          
          if [ -z "$GEMINI_TEXT" ]; then
              GEMINI_TEXT="**ERREUR : La revue IA a échoué.** Cause probable : formatage JSON ou mauvaise clé. **Réponse API brute :**\n\n$API_RESPONSE"
          fi

          echo "ai_review<<EOF_AI_REVIEW_OUTPUT" >> $GITHUB_OUTPUT
          echo "$GEMINI_TEXT" >> $GITHUB_OUTPUT
          echo "EOF_AI_REVIEW_OUTPUT" >> $GITHUB_OUTPUT
          
          COMMENT_BODY="## 🤖 Revue de Code (Alimenté par Gemini) 💖\n\n$GEMINI_TEXT\n\n---\n\n_Règle 'Love Component' : Composants en PascalCase, suffixe 'Love'._"
          
          cat << EOF_COMMENT_PAYLOAD > comment_payload.json
          {
            "body": "$COMMENT_BODY"
          }
          EOF_COMMENT_PAYLOAD
          
          curl -s -X POST "https://api.github.com/repos/$OWNER/$REPO/issues/$PR_NUMBER/comments" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Content-Type: application/json" \
            --data-binary "@comment_payload.json"
            
      - name: 📧 Send Failure Email Notification à l'équipe et l'auteur
        if: failure()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: 'ÉCHEC CI/CD pour PR #${{ github.event.pull_request.number }} par @${{ github.actor }}'
          body: |
            Bonjour @${{ github.event.pull_request.user.login }},
            
            Votre Pull Request #${{ github.event.pull_request.number }} a échoué au pipeline CI/CD (Build/Type Check/Linting).
            
            ---
            
            ## 🤖 Revue de l'Assistant IA (Gemini)
            
            ${{ steps.ai_review_step.outputs.ai_review }}
            
            ---
            
            Veuillez consulter les logs GitHub Actions pour les détails exacts de l'erreur ou le lien de la PR pour le commentaire: ${{ github.event.pull_request.html_url }}.
          to: ${{ secrets.TEAM_EMAIL_RECIPIENT }}
          from: ${{ secrets.MAIL_USERNAME }}